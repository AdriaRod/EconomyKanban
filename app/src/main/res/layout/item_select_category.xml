<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/item_card_background"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <!-- Título -->
    <TextView
        android:id="@+id/categoryName"
        style="@style/title_item_card"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Category"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/categoryCheckBox"
        app:layout_constraintBottom_toBottomOf="@id/categoryCheckBox"/>



    <!-- Checkbox -->
    <CheckBox
        android:id="@+id/categoryCheckBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <!-- Linea -->

    <View
        style="@style/line_item_card"
        android:layout_width="0dp"
        android:layout_height="1dp"
        app:layout_constraintTop_toBottomOf="@id/categoryCheckBox"
        android:layout_marginTop="-8dp"
        app:layout_constraintStart_toStartOf="@id/categoryName"
        app:layout_constraintEnd_toEndOf="@id/categoryCheckBox"/>


</androidx.constraintlayout.widget.ConstraintLayout>